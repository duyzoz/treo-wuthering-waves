{
  "name": "treo-wuthering-waves",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Persistent Wuthering Waves Discord rich presence",
  "main": "index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node index.js",
    "start:gc": "node --expose-gc index.js",
    "test": "node --check index.js && node --check official-bot.js && node --check observability.js && node --check presence-engine.js && node --check rest-governor.js && node --check incident-timeline.js && node --check remote-config.js && node --check storage-adapter.js && node --check ops-guard.js && node --check config-backup.js && node --check resource-governor.js && node test-waves.js"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "discord.js-selfbot-v13": "^3.7.1",
    "express": "^5.2.1"
  }
}
